{
  vim = {
    autocomplete = {
      blink-cmp = {
        enable = true;
        setupOpts = {
          # Setup function signature help with invoking function
          signature.enabled = true;
        };

        # Snippets
        friendly-snippets.enable = true;
      };
    };
  };
}
